# Production config for `eas update` / `eas build` exports (production mode).
# Expo loads this OVER .env in production, so OTA bundles don't pick up the local
# localhost URL. Public value (EXPO_PUBLIC_*), safe to commit — no secrets here.
EXPO_PUBLIC_API_URL=https://song-ranking-mobile-app-production.up.railway.app
